{
  "name": "dungeon-crawler-carl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Diablo-like dungeon crawler inspired by Dungeon Crawler Carl — vertical slice (single-player, local).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
